We calculate the distance of them by using Python numpy package. 

Different cluster algorithms successfully cross verify the black box method, that means potential relationship within container application can be mined from server log. Even though without any further information from service users, container service providers also have the capacities to effectively detect the dependency between containers.